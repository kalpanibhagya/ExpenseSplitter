	<p class="alert alert-info">
	<% if (notification.sender != nil)  %>
	<%= "#{notification.sender.name} : "  %>
	<% end  %>
		<%= notification.message %>
	<% if (notification.link != nil)  %>
	 <%= link_to('Link', root_path + notification.link , { :class=>"alert-link" } ) %>
	<% end  %>	
	<%= link_to('Dismiss', notification_read_path(:notification_id => notification.id) ) %>

	</p>
	
